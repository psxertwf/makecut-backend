name: makecut
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^0.13.6
  path_provider: ^2.1.2
  permission_handler: ^11.3.1
  youtube_explode_dart: ^1.12.4
  flutter_downloader: ^1.12.0
  ffmpeg_kit_flutter_full: 6.0.0  # ✅ Keep this version only (no podspec link here!)
  share_plus: ^7.2.2
  open_file: ^3.3.2
  process_run: ^0.13.3+1
  google_mobile_ads: ^5.3.1
  gallery_saver: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  